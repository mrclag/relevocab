import React from 'react';

import styled from 'styled-components';

const Search = () => {
  return <SearchWrapper>Search Items</SearchWrapper>;
};

export default Search;

const SearchWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;
